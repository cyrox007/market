/**
 * Контейнер полос шапки. Вынесен отдельно, потому что повторяется четыре раза
 * в трёх файлах, и любая правка отступов иначе разъезжается по ним.
 *
 * Замерено с макета 07.09.2026:
 *
 *   ширина до 1440
 *   отступы 100 на десктопе
 *   24 ниже 980
 *   16 ниже 550
 *
 * Пробовали уменьшать отступ между 980 и 1440, потому что там сжималась полоса.
 * Отменено: причина была не в отступах, а в поле поиска без `min-w-0` и в режиме
 * работы, который висел до 980. Оба места починены, отступы вернулись макетные.
 */
export const HEADER_CONTAINER = 'mx-auto w-full max-w-[1440px] px-[100px] max-md:px-6 max-vsm:px-4';
