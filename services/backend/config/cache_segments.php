<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Сегментация кэша: «горячие» и «холодные» данные
    |--------------------------------------------------------------------------
    |
    | Горячие данные (первые страницы, главное) — дольше живут в кэше и
    | прогреваются командой cache:warm-hot. Холодные — обычный TTL.
    |
    | Без привязки к региону кэшируются: списки товаров (index), поиск (search),
    | мета фильтров (filters_meta), категории (show/index/tree). Цены и
    | is_visible_in_region подставляются в API-ресурсах при отдаче по request.
    | Один кэш на все регионы — меньше ключей и быстрее попадания.
    |
    */

    /** TTL в секундах для «горячих» ключей (первые страницы, категории, блоки главной) */
    'hot_ttl' => (int) env('CACHE_HOT_TTL', 86400), // 24 часа

    /** TTL для остальных ключей (страницы 2+, фильтры, поиск) */
    'cold_ttl' => (int) env('CACHE_COLD_TTL', 36000), // 10 часов

    /** Включить предпрогрев по расписанию (если есть scheduler) */
    'warm_schedule' => env('CACHE_WARM_SCHEDULE', true),
];
